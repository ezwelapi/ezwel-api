<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezwel.htl.interfaces.server.repository.EzcCnclientFaclMarkMapper">
<!--
 * History : SQL Mapper Generated By iCodeManager Made by API Team KSW
 *
 * 버전          성명                   일자              		변경내용
 * =======       ================       ===================		=================
 * 0.0.1         CodeSkeleton (ksw)     2018-12-03 11:05:02 	신규작성 
 * 
 * @since 2018-12-03 11:05:02
 * @version 0.0.1
 * @author ksw
 * @see "DBIO Mapper"
 * -->
<insert id="insertEzcCnclientFaclMark" parameterType="com.ezwel.htl.interfaces.server.entities.EzcCnclientFaclMark">
INSERT INTO EZC_CNCLIENT_FACL_MARK(	/* 채널고객사 시설 마크 */
	 CLIENT_CD	/* 고객사 코드, VARCHAR2(20) */
	,GRP_FACL_CD	/* 그룹 시설 코드, NUMBER(0,22) */
	,CHANNEL_CD	/* 채널 코드, VARCHAR2(10) */
	,MARK_TYPE	/* 마크 유형, VARCHAR2(8) */
	,MARK_UNIT	/* 마크 단위, VARCHAR2(8) */
	,MARK_AMT	/* 마크 금액, NUMBER(0,22) */
	,REG_ID	/* 등록자 ID, VARCHAR2(20) */
	,REG_DT	/* 등록 일시, VARCHAR2(14) */
) VALUES (
	 #{clientCd, jdbcType=VARCHAR}
	,#{grpFaclCd, jdbcType=DECIMAL}
	,#{channelCd, jdbcType=VARCHAR}
	,#{markType, jdbcType=VARCHAR}
	,#{markUnit, jdbcType=VARCHAR}
	,#{markAmt, jdbcType=NUMERIC}
	,#{regId, jdbcType=VARCHAR}
	,#{regDt, jdbcType=VARCHAR}
)
</insert>

<select id="selectEzcCnclientFaclMark" parameterType="com.ezwel.htl.interfaces.server.entities.EzcCnclientFaclMark" resultType="com.ezwel.htl.interfaces.server.entities.EzcCnclientFaclMark">
SELECT
	 EZC_CNCLIENT_FACL_MARK.CLIENT_CD AS clientCd	/* 고객사 코드, VARCHAR2(20) */
	,EZC_CNCLIENT_FACL_MARK.GRP_FACL_CD AS grpFaclCd	/* 그룹 시설 코드, NUMBER(0,22) */
	,EZC_CNCLIENT_FACL_MARK.CHANNEL_CD AS channelCd	/* 채널 코드, VARCHAR2(10) */
	,EZC_CNCLIENT_FACL_MARK.MARK_TYPE AS markType	/* 마크 유형, VARCHAR2(8) */
	,EZC_CNCLIENT_FACL_MARK.MARK_UNIT AS markUnit	/* 마크 단위, VARCHAR2(8) */
	,EZC_CNCLIENT_FACL_MARK.MARK_AMT AS markAmt	/* 마크 금액, NUMBER(0,22) */
	,EZC_CNCLIENT_FACL_MARK.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
	,EZC_CNCLIENT_FACL_MARK.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
	,EZC_CNCLIENT_FACL_MARK.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
	,EZC_CNCLIENT_FACL_MARK.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
FROM
	EZC_CNCLIENT_FACL_MARK	/* 채널고객사 시설 마크 */
WHERE 1=1
	AND EZC_CNCLIENT_FACL_MARK.CLIENT_CD = #{clientCd, jdbcType=VARCHAR}	/* 고객사 코드, VARCHAR2(20) */
	AND EZC_CNCLIENT_FACL_MARK.GRP_FACL_CD = #{grpFaclCd, jdbcType=DECIMAL}	/* 그룹 시설 코드, NUMBER(0,22) */
	AND EZC_CNCLIENT_FACL_MARK.CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
</select>

<select id="selectCountEzcCnclientFaclMark" parameterType="com.ezwel.htl.interfaces.server.entities.EzcCnclientFaclMark" resultType="java.lang.Integer">
SELECT 
	COUNT(1)
FROM
	EZC_CNCLIENT_FACL_MARK	/* 채널고객사 시설 마크 */
WHERE 1=1
	<if test="clientCd != null and clientCd != ''">
	AND EZC_CNCLIENT_FACL_MARK.CLIENT_CD = #{clientCd, jdbcType=VARCHAR}	/* 고객사 코드, VARCHAR2(20) */
	</if>
	<if test="grpFaclCd != null and grpFaclCd gt 0">
	AND EZC_CNCLIENT_FACL_MARK.GRP_FACL_CD = #{grpFaclCd, jdbcType=DECIMAL}	/* 그룹 시설 코드, NUMBER(0,22) */
	</if>
	<if test="channelCd != null and channelCd != ''">
	AND EZC_CNCLIENT_FACL_MARK.CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
	</if>
</select>

<select id="selectListEzcCnclientFaclMark" resultType="com.ezwel.htl.interfaces.server.entities.EzcCnclientFaclMark">
SELECT * FROM (
	SELECT ROWNUM AS ROW__NUM, RECORDS.* FROM (
		/* #### SQL Body [[ ################# */
		SELECT
			 EZC_CNCLIENT_FACL_MARK.CLIENT_CD AS clientCd	/* 고객사 코드, VARCHAR2(20) */
			,EZC_CNCLIENT_FACL_MARK.GRP_FACL_CD AS grpFaclCd	/* 그룹 시설 코드, NUMBER(0,22) */
			,EZC_CNCLIENT_FACL_MARK.CHANNEL_CD AS channelCd	/* 채널 코드, VARCHAR2(10) */
			,EZC_CNCLIENT_FACL_MARK.MARK_TYPE AS markType	/* 마크 유형, VARCHAR2(8) */
			,EZC_CNCLIENT_FACL_MARK.MARK_UNIT AS markUnit	/* 마크 단위, VARCHAR2(8) */
			,EZC_CNCLIENT_FACL_MARK.MARK_AMT AS markAmt	/* 마크 금액, NUMBER(0,22) */
			,EZC_CNCLIENT_FACL_MARK.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
			,EZC_CNCLIENT_FACL_MARK.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
			,EZC_CNCLIENT_FACL_MARK.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
			,EZC_CNCLIENT_FACL_MARK.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
		FROM
			EZC_CNCLIENT_FACL_MARK	/* 채널고객사 시설 마크 */
		WHERE 1=1
			<if test="param.clientCd != null and param.clientCd != ''">
			AND EZC_CNCLIENT_FACL_MARK.CLIENT_CD = #{param.clientCd, jdbcType=VARCHAR}	/* 고객사 코드, VARCHAR2(20) */
			</if>
			<if test="param.grpFaclCd != null and param.grpFaclCd gt 0">
			AND EZC_CNCLIENT_FACL_MARK.GRP_FACL_CD = #{param.grpFaclCd, jdbcType=DECIMAL}	/* 그룹 시설 코드, NUMBER(0,22) */
			</if>
			<if test="param.channelCd != null and param.channelCd != ''">
			AND EZC_CNCLIENT_FACL_MARK.CHANNEL_CD = #{param.channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
			</if>
		ORDER BY
			EZC_CNCLIENT_FACL_MARK.CLIENT_CD DESC
		/* #### SQL Body ]] ################# */
	) RECORDS
	WHERE ROWNUM &lt;= ((#{param.pageNum, jdbcType=NUMERIC}*#{param.pageCount, jdbcType=NUMERIC})+1) 
)
WHERE ROW__NUM &gt; (#{param.pageNum, jdbcType=NUMERIC}-1)*#{param.pageCount, jdbcType=NUMERIC}
</select>

<update id="updateEzcCnclientFaclMark" parameterType="com.ezwel.htl.interfaces.server.entities.EzcCnclientFaclMark">
UPDATE EZC_CNCLIENT_FACL_MARK SET	/* 채널고객사 시설 마크 */
	 MARK_TYPE = #{markType, jdbcType=VARCHAR}	/* 마크 유형, VARCHAR2(8) */
	,MARK_UNIT = #{markUnit, jdbcType=VARCHAR}	/* 마크 단위, VARCHAR2(8) */
	,MARK_AMT = #{markAmt, jdbcType=NUMERIC}	/* 마크 금액, NUMBER(0,22) */
	,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
	,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
WHERE 1=1
	AND CLIENT_CD = #{clientCd, jdbcType=VARCHAR}	/* 고객사 코드, VARCHAR2(20) */
	AND GRP_FACL_CD = #{grpFaclCd, jdbcType=DECIMAL}	/* 그룹 시설 코드, NUMBER(0,22) */
	AND CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
</update>

<update id="mergeEzcCnclientFaclMark" parameterType="com.ezwel.htl.interfaces.server.entities.EzcCnclientFaclMark">
MERGE INTO EZC_CNCLIENT_FACL_MARK /* 채널고객사 시설 마크 */
	USING DUAL ON (
		CLIENT_CD = #{clientCd, jdbcType=VARCHAR}	/* 고객사 코드, VARCHAR2(20) */
		AND GRP_FACL_CD = #{grpFaclCd, jdbcType=DECIMAL}	/* 그룹 시설 코드, NUMBER(0,22) */
		AND CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
	)
	WHEN MATCHED THEN
		UPDATE SET
			 MARK_TYPE = #{markType, jdbcType=VARCHAR}	/* 마크 유형, VARCHAR2(8) */
			,MARK_UNIT = #{markUnit, jdbcType=VARCHAR}	/* 마크 단위, VARCHAR2(8) */
			,MARK_AMT = #{markAmt, jdbcType=NUMERIC}	/* 마크 금액, NUMBER(0,22) */
			,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
			,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
	WHEN NOT MATCHED THEN
		INSERT (
			 CLIENT_CD	/* 고객사 코드, VARCHAR2(20) */
			,GRP_FACL_CD	/* 그룹 시설 코드, NUMBER(0,22) */
			,CHANNEL_CD	/* 채널 코드, VARCHAR2(10) */
			,MARK_TYPE	/* 마크 유형, VARCHAR2(8) */
			,MARK_UNIT	/* 마크 단위, VARCHAR2(8) */
			,MARK_AMT	/* 마크 금액, NUMBER(0,22) */
			,REG_ID	/* 등록자 ID, VARCHAR2(20) */
			,REG_DT	/* 등록 일시, VARCHAR2(14) */
		) VALUES (
			 #{clientCd, jdbcType=VARCHAR}
			,#{grpFaclCd, jdbcType=DECIMAL}
			,#{channelCd, jdbcType=VARCHAR}
			,#{markType, jdbcType=VARCHAR}
			,#{markUnit, jdbcType=VARCHAR}
			,#{markAmt, jdbcType=NUMERIC}
			,#{regId, jdbcType=VARCHAR}
			,#{regDt, jdbcType=VARCHAR}
		)
</update>

<delete id="deleteEzcCnclientFaclMark" parameterType="com.ezwel.htl.interfaces.server.entities.EzcCnclientFaclMark">
DELETE FROM EZC_CNCLIENT_FACL_MARK /* 채널고객사 시설 마크 */
WHERE 1=1
	AND CLIENT_CD = #{clientCd, jdbcType=VARCHAR}	/* 고객사 코드, VARCHAR2(20) */
	AND GRP_FACL_CD = #{grpFaclCd, jdbcType=DECIMAL}	/* 그룹 시설 코드, NUMBER(0,22) */
	AND CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
</delete>


</mapper>
