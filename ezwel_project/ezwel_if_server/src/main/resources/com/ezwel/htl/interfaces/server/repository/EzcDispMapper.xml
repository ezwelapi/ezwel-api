<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezwel.htl.interfaces.server.repository.EzcDispMapper">
<!--
 * History : SQL Mapper Generated By iCodeManager Made by API Team KSW
 *
 * 버전          성명                   일자              		변경내용
 * =======       ================       ===================		=================
 * 0.0.1         CodeSkeleton (ksw)     2018-12-03 11:05:02 	신규작성 
 * 
 * @since 2018-12-03 11:05:02
 * @version 0.0.1
 * @author ksw
 * @see "DBIO Mapper"
 * -->
<insert id="insertEzcDisp" parameterType="com.ezwel.htl.interfaces.server.entities.EzcDisp">
INSERT INTO EZC_DISP(	/* 전시 */
	 DISP_CD	/* 전시 코드, NUMBER(0,22) */
	,DISP_NM	/* 전시 명, VARCHAR2(100) */
	,DISP_DESC	/* 전시 설명, VARCHAR2(4000) */
	,IMG_PATH	/* 이미지 경로, VARCHAR2(100) */
	,IMG_SYS_FILENM	/* 이미지 시스템 파일명, VARCHAR2(100) */
	,IMG_FILENM	/* 이미지 파일명, VARCHAR2(100) */
	,URL	/* URL, VARCHAR2(200) */
	,IMG_HTML	/* 이미지 HTML, VARCHAR2(4000) */
	,REG_ID	/* 등록자 ID, VARCHAR2(20) */
	,REG_DT	/* 등록 일시, VARCHAR2(14) */
) VALUES (
	 #{dispCd, jdbcType=DECIMAL}
	,#{dispNm, jdbcType=VARCHAR}
	,#{dispDesc, jdbcType=VARCHAR}
	,#{imgPath, jdbcType=VARCHAR}
	,#{imgSysFilenm, jdbcType=VARCHAR}
	,#{imgFilenm, jdbcType=VARCHAR}
	,#{url, jdbcType=VARCHAR}
	,#{imgHtml, jdbcType=VARCHAR}
	,#{regId, jdbcType=VARCHAR}
	,#{regDt, jdbcType=VARCHAR}
)
</insert>

<select id="selectEzcDisp" parameterType="com.ezwel.htl.interfaces.server.entities.EzcDisp" resultType="com.ezwel.htl.interfaces.server.entities.EzcDisp">
SELECT
	 EZC_DISP.DISP_CD AS dispCd	/* 전시 코드, NUMBER(0,22) */
	,EZC_DISP.DISP_NM AS dispNm	/* 전시 명, VARCHAR2(100) */
	,EZC_DISP.DISP_DESC AS dispDesc	/* 전시 설명, VARCHAR2(4000) */
	,EZC_DISP.IMG_PATH AS imgPath	/* 이미지 경로, VARCHAR2(100) */
	,EZC_DISP.IMG_SYS_FILENM AS imgSysFilenm	/* 이미지 시스템 파일명, VARCHAR2(100) */
	,EZC_DISP.IMG_FILENM AS imgFilenm	/* 이미지 파일명, VARCHAR2(100) */
	,EZC_DISP.URL AS url	/* URL, VARCHAR2(200) */
	,EZC_DISP.IMG_HTML AS imgHtml	/* 이미지 HTML, VARCHAR2(4000) */
	,EZC_DISP.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
	,EZC_DISP.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
	,EZC_DISP.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
	,EZC_DISP.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
FROM
	EZC_DISP	/* 전시 */
WHERE 1=1
	AND EZC_DISP.DISP_CD = #{dispCd, jdbcType=DECIMAL}	/* 전시 코드, NUMBER(0,22) */
</select>

<select id="selectCountEzcDisp" parameterType="com.ezwel.htl.interfaces.server.entities.EzcDisp" resultType="java.lang.Integer">
SELECT 
	COUNT(1)
FROM
	EZC_DISP	/* 전시 */
WHERE 1=1
	<if test="dispCd != null and dispCd gt 0">
	AND EZC_DISP.DISP_CD = #{dispCd, jdbcType=DECIMAL}	/* 전시 코드, NUMBER(0,22) */
	</if>
	<if test="dispDesc != null and dispDesc != ''">
	AND EZC_DISP.DISP_DESC LIKE '%' || #{dispDesc, jdbcType=VARCHAR} || '%'	/* 전시 설명, VARCHAR2(4000) */
	</if>
	<if test="imgHtml != null and imgHtml != ''">
	AND EZC_DISP.IMG_HTML LIKE '%' || #{imgHtml, jdbcType=VARCHAR} || '%'	/* 이미지 HTML, VARCHAR2(4000) */
	</if>
</select>

<select id="selectListEzcDisp" resultType="com.ezwel.htl.interfaces.server.entities.EzcDisp">
SELECT * FROM (
	SELECT ROWNUM AS ROW__NUM, RECORDS.* FROM (
		/* #### SQL Body [[ ################# */
		SELECT
			 EZC_DISP.DISP_CD AS dispCd	/* 전시 코드, NUMBER(0,22) */
			,EZC_DISP.DISP_NM AS dispNm	/* 전시 명, VARCHAR2(100) */
			,EZC_DISP.DISP_DESC AS dispDesc	/* 전시 설명, VARCHAR2(4000) */
			,EZC_DISP.IMG_PATH AS imgPath	/* 이미지 경로, VARCHAR2(100) */
			,EZC_DISP.IMG_SYS_FILENM AS imgSysFilenm	/* 이미지 시스템 파일명, VARCHAR2(100) */
			,EZC_DISP.IMG_FILENM AS imgFilenm	/* 이미지 파일명, VARCHAR2(100) */
			,EZC_DISP.URL AS url	/* URL, VARCHAR2(200) */
			,EZC_DISP.IMG_HTML AS imgHtml	/* 이미지 HTML, VARCHAR2(4000) */
			,EZC_DISP.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
			,EZC_DISP.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
			,EZC_DISP.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
			,EZC_DISP.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
		FROM
			EZC_DISP	/* 전시 */
		WHERE 1=1
			<if test="param.dispCd != null and param.dispCd gt 0">
			AND EZC_DISP.DISP_CD = #{param.dispCd, jdbcType=DECIMAL}	/* 전시 코드, NUMBER(0,22) */
			</if>
			<if test="param.dispDesc != null and param.dispDesc != ''">
			AND EZC_DISP.DISP_DESC LIKE '%' || #{param.dispDesc, jdbcType=VARCHAR} || '%'	/* 전시 설명, VARCHAR2(4000) */
			</if>
			<if test="param.imgHtml != null and param.imgHtml != ''">
			AND EZC_DISP.IMG_HTML LIKE '%' || #{param.imgHtml, jdbcType=VARCHAR} || '%'	/* 이미지 HTML, VARCHAR2(4000) */
			</if>
		ORDER BY
			EZC_DISP.DISP_CD DESC
		/* #### SQL Body ]] ################# */
	) RECORDS
	WHERE ROWNUM &lt;= ((#{param.pageNum, jdbcType=NUMERIC}*#{param.pageCount, jdbcType=NUMERIC})+1) 
)
WHERE ROW__NUM &gt; (#{param.pageNum, jdbcType=NUMERIC}-1)*#{param.pageCount, jdbcType=NUMERIC}
</select>

<update id="updateEzcDisp" parameterType="com.ezwel.htl.interfaces.server.entities.EzcDisp">
UPDATE EZC_DISP SET	/* 전시 */
	 DISP_NM = #{dispNm, jdbcType=VARCHAR}	/* 전시 명, VARCHAR2(100) */
	,DISP_DESC = #{dispDesc, jdbcType=VARCHAR}	/* 전시 설명, VARCHAR2(4000) */
	,IMG_PATH = #{imgPath, jdbcType=VARCHAR}	/* 이미지 경로, VARCHAR2(100) */
	,IMG_SYS_FILENM = #{imgSysFilenm, jdbcType=VARCHAR}	/* 이미지 시스템 파일명, VARCHAR2(100) */
	,IMG_FILENM = #{imgFilenm, jdbcType=VARCHAR}	/* 이미지 파일명, VARCHAR2(100) */
	,URL = #{url, jdbcType=VARCHAR}	/* URL, VARCHAR2(200) */
	,IMG_HTML = #{imgHtml, jdbcType=VARCHAR}	/* 이미지 HTML, VARCHAR2(4000) */
	,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
	,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
WHERE 1=1
	AND DISP_CD = #{dispCd, jdbcType=DECIMAL}	/* 전시 코드, NUMBER(0,22) */
</update>

<update id="mergeEzcDisp" parameterType="com.ezwel.htl.interfaces.server.entities.EzcDisp">
MERGE INTO EZC_DISP /* 전시 */
	USING DUAL ON (
		DISP_CD = #{dispCd, jdbcType=DECIMAL}	/* 전시 코드, NUMBER(0,22) */
	)
	WHEN MATCHED THEN
		UPDATE SET
			 DISP_NM = #{dispNm, jdbcType=VARCHAR}	/* 전시 명, VARCHAR2(100) */
			,DISP_DESC = #{dispDesc, jdbcType=VARCHAR}	/* 전시 설명, VARCHAR2(4000) */
			,IMG_PATH = #{imgPath, jdbcType=VARCHAR}	/* 이미지 경로, VARCHAR2(100) */
			,IMG_SYS_FILENM = #{imgSysFilenm, jdbcType=VARCHAR}	/* 이미지 시스템 파일명, VARCHAR2(100) */
			,IMG_FILENM = #{imgFilenm, jdbcType=VARCHAR}	/* 이미지 파일명, VARCHAR2(100) */
			,URL = #{url, jdbcType=VARCHAR}	/* URL, VARCHAR2(200) */
			,IMG_HTML = #{imgHtml, jdbcType=VARCHAR}	/* 이미지 HTML, VARCHAR2(4000) */
			,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
			,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
	WHEN NOT MATCHED THEN
		INSERT (
			 DISP_CD	/* 전시 코드, NUMBER(0,22) */
			,DISP_NM	/* 전시 명, VARCHAR2(100) */
			,DISP_DESC	/* 전시 설명, VARCHAR2(4000) */
			,IMG_PATH	/* 이미지 경로, VARCHAR2(100) */
			,IMG_SYS_FILENM	/* 이미지 시스템 파일명, VARCHAR2(100) */
			,IMG_FILENM	/* 이미지 파일명, VARCHAR2(100) */
			,URL	/* URL, VARCHAR2(200) */
			,IMG_HTML	/* 이미지 HTML, VARCHAR2(4000) */
			,REG_ID	/* 등록자 ID, VARCHAR2(20) */
			,REG_DT	/* 등록 일시, VARCHAR2(14) */
		) VALUES (
			 #{dispCd, jdbcType=DECIMAL}
			,#{dispNm, jdbcType=VARCHAR}
			,#{dispDesc, jdbcType=VARCHAR}
			,#{imgPath, jdbcType=VARCHAR}
			,#{imgSysFilenm, jdbcType=VARCHAR}
			,#{imgFilenm, jdbcType=VARCHAR}
			,#{url, jdbcType=VARCHAR}
			,#{imgHtml, jdbcType=VARCHAR}
			,#{regId, jdbcType=VARCHAR}
			,#{regDt, jdbcType=VARCHAR}
		)
</update>

<delete id="deleteEzcDisp" parameterType="com.ezwel.htl.interfaces.server.entities.EzcDisp">
DELETE FROM EZC_DISP /* 전시 */
WHERE 1=1
	AND DISP_CD = #{dispCd, jdbcType=DECIMAL}	/* 전시 코드, NUMBER(0,22) */
</delete>


</mapper>
