<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezwel.htl.interfaces.server.repository.EzcMgrLogMapper">
<!--
 * History : SQL Mapper Generated By iCodeManager Made by API Team KSW
 *
 * 버전          성명                   일자              		변경내용
 * =======       ================       ===================		=================
 * 0.0.1         CodeSkeleton (ksw)     2018-12-03 11:05:03 	신규작성 
 * 
 * @since 2018-12-03 11:05:03
 * @version 0.0.1
 * @author ksw
 * @see "DBIO Mapper"
 * -->
<insert id="insertEzcMgrLog" parameterType="com.ezwel.htl.interfaces.server.entities.EzcMgrLog">
INSERT INTO EZC_MGR_LOG(	/* 운영자 로그 */
	 LOG_SEQ	/* 로그 일련번호, NUMBER(0,22) */
	,MGR_ID	/* 운영자 ID, VARCHAR2(20) */
	,LOG_TYPE	/* 로그 유형, VARCHAR2(8) */
	,CONTENT	/* 내용, VARCHAR2(1000) */
	,CONN_IP	/* 접속 IP, VARCHAR2(15) */
	,REG_ID	/* 등록자 ID, VARCHAR2(20) */
	,REG_DT	/* 등록 일시, VARCHAR2(14) */
) VALUES (
	 #{logSeq, jdbcType=DECIMAL}
	,#{mgrId, jdbcType=VARCHAR}
	,#{logType, jdbcType=VARCHAR}
	,#{content, jdbcType=VARCHAR}
	,#{connIp, jdbcType=VARCHAR}
	,#{regId, jdbcType=VARCHAR}
	,#{regDt, jdbcType=VARCHAR}
)
</insert>

<select id="selectEzcMgrLog" parameterType="com.ezwel.htl.interfaces.server.entities.EzcMgrLog" resultType="com.ezwel.htl.interfaces.server.entities.EzcMgrLog">
SELECT
	 EZC_MGR_LOG.LOG_SEQ AS logSeq	/* 로그 일련번호, NUMBER(0,22) */
	,EZC_MGR_LOG.MGR_ID AS mgrId	/* 운영자 ID, VARCHAR2(20) */
	,EZC_MGR_LOG.LOG_TYPE AS logType	/* 로그 유형, VARCHAR2(8) */
	,EZC_MGR_LOG.CONTENT AS content	/* 내용, VARCHAR2(1000) */
	,EZC_MGR_LOG.CONN_IP AS connIp	/* 접속 IP, VARCHAR2(15) */
	,EZC_MGR_LOG.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
	,EZC_MGR_LOG.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
	,EZC_MGR_LOG.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
	,EZC_MGR_LOG.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
FROM
	 EZC_MGR_LOG	/* 운영자 로그 */
	,EZC_MGR	/* 운영자 정보 */
	,EZC_CLIENT_INFO	/* 고객사 */
	,EZC_PARTNER	/* 제휴사 정보 */
WHERE 1=1
	AND EZC_MGR_LOG.MGR_ID = EZC_MGR.MGR_ID
	AND EZC_MGR.CLIENT_CD = EZC_CLIENT_INFO.CLIENT_CD
	AND EZC_MGR.PARTNER_CD = EZC_PARTNER.PARTNER_CD
	AND EZC_MGR_LOG.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_MGR.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_CLIENT_INFO.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_PARTNER.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_MGR_LOG.LOG_SEQ = #{logSeq, jdbcType=DECIMAL}	/* 로그 일련번호, NUMBER(0,22) */
	AND EZC_MGR_LOG.MGR_ID = #{mgrId, jdbcType=VARCHAR}	/* 운영자 ID, VARCHAR2(20) */
</select>

<select id="selectCountEzcMgrLog" parameterType="com.ezwel.htl.interfaces.server.entities.EzcMgrLog" resultType="java.lang.Integer">
SELECT 
	COUNT(1)
FROM
	 EZC_MGR_LOG	/* 운영자 로그 */
	,EZC_MGR	/* 운영자 정보 */
	,EZC_CLIENT_INFO	/* 고객사 */
	,EZC_PARTNER	/* 제휴사 정보 */
WHERE 1=1
	AND EZC_MGR_LOG.MGR_ID = EZC_MGR.MGR_ID
	AND EZC_MGR.CLIENT_CD = EZC_CLIENT_INFO.CLIENT_CD
	AND EZC_MGR.PARTNER_CD = EZC_PARTNER.PARTNER_CD
	AND EZC_MGR_LOG.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_MGR.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_CLIENT_INFO.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_PARTNER.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	<if test="logSeq != null and logSeq gt 0">
	AND EZC_MGR_LOG.LOG_SEQ = #{logSeq, jdbcType=DECIMAL}	/* 로그 일련번호, NUMBER(0,22) */
	</if>
	<if test="mgrId != null and mgrId != ''">
	AND EZC_MGR_LOG.MGR_ID = #{mgrId, jdbcType=VARCHAR}	/* 운영자 ID, VARCHAR2(20) */
	</if>
	<if test="content != null and content != ''">
	AND EZC_MGR_LOG.CONTENT LIKE '%' || #{content, jdbcType=VARCHAR} || '%'	/* 내용, VARCHAR2(1000) */
	</if>
</select>

<select id="selectListEzcMgrLog" resultType="com.ezwel.htl.interfaces.server.entities.EzcMgrLog">
SELECT * FROM (
	SELECT ROWNUM AS ROW__NUM, RECORDS.* FROM (
		/* #### SQL Body [[ ################# */
		SELECT
			 EZC_MGR_LOG.LOG_SEQ AS logSeq	/* 로그 일련번호, NUMBER(0,22) */
			,EZC_MGR_LOG.MGR_ID AS mgrId	/* 운영자 ID, VARCHAR2(20) */
			,EZC_MGR_LOG.LOG_TYPE AS logType	/* 로그 유형, VARCHAR2(8) */
			,EZC_MGR_LOG.CONTENT AS content	/* 내용, VARCHAR2(1000) */
			,EZC_MGR_LOG.CONN_IP AS connIp	/* 접속 IP, VARCHAR2(15) */
			,EZC_MGR_LOG.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
			,EZC_MGR_LOG.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
			,EZC_MGR_LOG.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
			,EZC_MGR_LOG.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
		FROM
			 EZC_MGR_LOG	/* 운영자 로그 */
			,EZC_MGR	/* 운영자 정보 */
			,EZC_CLIENT_INFO	/* 고객사 */
			,EZC_PARTNER	/* 제휴사 정보 */
		WHERE 1=1
			AND EZC_MGR_LOG.MGR_ID = EZC_MGR.MGR_ID
			AND EZC_MGR.CLIENT_CD = EZC_CLIENT_INFO.CLIENT_CD
			AND EZC_MGR.PARTNER_CD = EZC_PARTNER.PARTNER_CD
			AND EZC_MGR_LOG.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			AND EZC_MGR.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			AND EZC_CLIENT_INFO.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			AND EZC_PARTNER.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			<if test="param.logSeq != null and param.logSeq gt 0">
			AND EZC_MGR_LOG.LOG_SEQ = #{param.logSeq, jdbcType=DECIMAL}	/* 로그 일련번호, NUMBER(0,22) */
			</if>
			<if test="param.mgrId != null and param.mgrId != ''">
			AND EZC_MGR_LOG.MGR_ID = #{param.mgrId, jdbcType=VARCHAR}	/* 운영자 ID, VARCHAR2(20) */
			</if>
			<if test="param.content != null and param.content != ''">
			AND EZC_MGR_LOG.CONTENT LIKE '%' || #{param.content, jdbcType=VARCHAR} || '%'	/* 내용, VARCHAR2(1000) */
			</if>
		ORDER BY
			EZC_MGR_LOG.LOG_SEQ DESC
		/* #### SQL Body ]] ################# */
	) RECORDS
	WHERE ROWNUM &lt;= ((#{param.pageNum, jdbcType=NUMERIC}*#{param.pageCount, jdbcType=NUMERIC})+1) 
)
WHERE ROW__NUM &gt; (#{param.pageNum, jdbcType=NUMERIC}-1)*#{param.pageCount, jdbcType=NUMERIC}
</select>

<update id="updateEzcMgrLog" parameterType="com.ezwel.htl.interfaces.server.entities.EzcMgrLog">
UPDATE EZC_MGR_LOG SET	/* 운영자 로그 */
	 LOG_TYPE = #{logType, jdbcType=VARCHAR}	/* 로그 유형, VARCHAR2(8) */
	,CONTENT = #{content, jdbcType=VARCHAR}	/* 내용, VARCHAR2(1000) */
	,CONN_IP = #{connIp, jdbcType=VARCHAR}	/* 접속 IP, VARCHAR2(15) */
	,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
	,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
WHERE 1=1
	AND LOG_SEQ = #{logSeq, jdbcType=DECIMAL}	/* 로그 일련번호, NUMBER(0,22) */
	AND MGR_ID = #{mgrId, jdbcType=VARCHAR}	/* 운영자 ID, VARCHAR2(20) */
</update>

<update id="mergeEzcMgrLog" parameterType="com.ezwel.htl.interfaces.server.entities.EzcMgrLog">
MERGE INTO EZC_MGR_LOG /* 운영자 로그 */
	USING DUAL ON (
		LOG_SEQ = #{logSeq, jdbcType=DECIMAL}	/* 로그 일련번호, NUMBER(0,22) */
		AND MGR_ID = #{mgrId, jdbcType=VARCHAR}	/* 운영자 ID, VARCHAR2(20) */
	)
	WHEN MATCHED THEN
		UPDATE SET
			 LOG_TYPE = #{logType, jdbcType=VARCHAR}	/* 로그 유형, VARCHAR2(8) */
			,CONTENT = #{content, jdbcType=VARCHAR}	/* 내용, VARCHAR2(1000) */
			,CONN_IP = #{connIp, jdbcType=VARCHAR}	/* 접속 IP, VARCHAR2(15) */
			,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
			,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
	WHEN NOT MATCHED THEN
		INSERT (
			 LOG_SEQ	/* 로그 일련번호, NUMBER(0,22) */
			,MGR_ID	/* 운영자 ID, VARCHAR2(20) */
			,LOG_TYPE	/* 로그 유형, VARCHAR2(8) */
			,CONTENT	/* 내용, VARCHAR2(1000) */
			,CONN_IP	/* 접속 IP, VARCHAR2(15) */
			,REG_ID	/* 등록자 ID, VARCHAR2(20) */
			,REG_DT	/* 등록 일시, VARCHAR2(14) */
		) VALUES (
			 #{logSeq, jdbcType=DECIMAL}
			,#{mgrId, jdbcType=VARCHAR}
			,#{logType, jdbcType=VARCHAR}
			,#{content, jdbcType=VARCHAR}
			,#{connIp, jdbcType=VARCHAR}
			,#{regId, jdbcType=VARCHAR}
			,#{regDt, jdbcType=VARCHAR}
		)
</update>

<delete id="deleteEzcMgrLog" parameterType="com.ezwel.htl.interfaces.server.entities.EzcMgrLog">
DELETE FROM EZC_MGR_LOG /* 운영자 로그 */
WHERE 1=1
	AND LOG_SEQ = #{logSeq, jdbcType=DECIMAL}	/* 로그 일련번호, NUMBER(0,22) */
	AND MGR_ID = #{mgrId, jdbcType=VARCHAR}	/* 운영자 ID, VARCHAR2(20) */
</delete>


</mapper>
