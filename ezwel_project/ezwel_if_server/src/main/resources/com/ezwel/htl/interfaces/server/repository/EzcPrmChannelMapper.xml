<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezwel.htl.interfaces.server.repository.EzcPrmChannelMapper">
<!--
 * History : SQL Mapper Generated By iCodeManager Made by API Team KSW
 *
 * 버전          성명                   일자              		변경내용
 * =======       ================       ===================		=================
 * 0.0.1         CodeSkeleton (ksw)     2018-12-03 11:05:04 	신규작성 
 * 
 * @since 2018-12-03 11:05:04
 * @version 0.0.1
 * @author ksw
 * @see "DBIO Mapper"
 * -->
<insert id="insertEzcPrmChannel" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmChannel">
INSERT INTO EZC_PRM_CHANNEL(	/* 프로모션 채널 */
	 PRM_CD	/* 프로모션 코드, VARCHAR2(10) */
	,CHANNEL_CD	/* 채널 코드, VARCHAR2(10) */
	,REG_ID	/* 등록자 ID, VARCHAR2(20) */
	,REG_DT	/* 등록 일시, VARCHAR2(14) */
) VALUES (
	 #{prmCd, jdbcType=VARCHAR}
	,#{channelCd, jdbcType=VARCHAR}
	,#{regId, jdbcType=VARCHAR}
	,#{regDt, jdbcType=VARCHAR}
)
</insert>

<select id="selectEzcPrmChannel" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmChannel" resultType="com.ezwel.htl.interfaces.server.entities.EzcPrmChannel">
SELECT
	 EZC_PRM_CHANNEL.PRM_CD AS prmCd	/* 프로모션 코드, VARCHAR2(10) */
	,EZC_PRM_CHANNEL.CHANNEL_CD AS channelCd	/* 채널 코드, VARCHAR2(10) */
	,EZC_PRM_CHANNEL.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
	,EZC_PRM_CHANNEL.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
FROM
	 EZC_PRM_CHANNEL	/* 프로모션 채널 */
	,EZC_CHANNEL_MNG	/* 채널 */
	,EZC_PRM_MNG	/* 프로모션 관리 */
WHERE 1=1
	AND EZC_PRM_CHANNEL.CHANNEL_CD = EZC_CHANNEL_MNG.CHANNEL_CD
	AND EZC_PRM_CHANNEL.PRM_CD = EZC_PRM_MNG.PRM_CD
	AND EZC_PRM_CHANNEL.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_CHANNEL_MNG.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_PRM_MNG.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_PRM_CHANNEL.PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
	AND EZC_PRM_CHANNEL.CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
</select>

<select id="selectCountEzcPrmChannel" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmChannel" resultType="java.lang.Integer">
SELECT 
	COUNT(1)
FROM
	 EZC_PRM_CHANNEL	/* 프로모션 채널 */
	,EZC_CHANNEL_MNG	/* 채널 */
	,EZC_PRM_MNG	/* 프로모션 관리 */
WHERE 1=1
	AND EZC_PRM_CHANNEL.CHANNEL_CD = EZC_CHANNEL_MNG.CHANNEL_CD
	AND EZC_PRM_CHANNEL.PRM_CD = EZC_PRM_MNG.PRM_CD
	AND EZC_PRM_CHANNEL.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_CHANNEL_MNG.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_PRM_MNG.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	<if test="prmCd != null and prmCd != ''">
	AND EZC_PRM_CHANNEL.PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
	</if>
	<if test="channelCd != null and channelCd != ''">
	AND EZC_PRM_CHANNEL.CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
	</if>
</select>

<select id="selectListEzcPrmChannel" resultType="com.ezwel.htl.interfaces.server.entities.EzcPrmChannel">
SELECT * FROM (
	SELECT ROWNUM AS ROW__NUM, RECORDS.* FROM (
		/* #### SQL Body [[ ################# */
		SELECT
			 EZC_PRM_CHANNEL.PRM_CD AS prmCd	/* 프로모션 코드, VARCHAR2(10) */
			,EZC_PRM_CHANNEL.CHANNEL_CD AS channelCd	/* 채널 코드, VARCHAR2(10) */
			,EZC_PRM_CHANNEL.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
			,EZC_PRM_CHANNEL.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
		FROM
			 EZC_PRM_CHANNEL	/* 프로모션 채널 */
			,EZC_CHANNEL_MNG	/* 채널 */
			,EZC_PRM_MNG	/* 프로모션 관리 */
		WHERE 1=1
			AND EZC_PRM_CHANNEL.CHANNEL_CD = EZC_CHANNEL_MNG.CHANNEL_CD
			AND EZC_PRM_CHANNEL.PRM_CD = EZC_PRM_MNG.PRM_CD
			AND EZC_PRM_CHANNEL.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			AND EZC_CHANNEL_MNG.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			AND EZC_PRM_MNG.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			<if test="param.prmCd != null and param.prmCd != ''">
			AND EZC_PRM_CHANNEL.PRM_CD = #{param.prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
			</if>
			<if test="param.channelCd != null and param.channelCd != ''">
			AND EZC_PRM_CHANNEL.CHANNEL_CD = #{param.channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
			</if>
		ORDER BY
			EZC_PRM_CHANNEL.PRM_CD DESC
		/* #### SQL Body ]] ################# */
	) RECORDS
	WHERE ROWNUM &lt;= ((#{param.pageNum, jdbcType=NUMERIC}*#{param.pageCount, jdbcType=NUMERIC})+1) 
)
WHERE ROW__NUM &gt; (#{param.pageNum, jdbcType=NUMERIC}-1)*#{param.pageCount, jdbcType=NUMERIC}
</select>

<update id="updateEzcPrmChannel" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmChannel">
UPDATE EZC_PRM_CHANNEL SET	/* 프로모션 채널 */
WHERE 1=1
	AND PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
	AND CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
</update>

<update id="mergeEzcPrmChannel" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmChannel">
MERGE INTO EZC_PRM_CHANNEL /* 프로모션 채널 */
	USING DUAL ON (
		PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
		AND CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
	)
	WHEN MATCHED THEN
		UPDATE SET
	WHEN NOT MATCHED THEN
		INSERT (
			 PRM_CD	/* 프로모션 코드, VARCHAR2(10) */
			,CHANNEL_CD	/* 채널 코드, VARCHAR2(10) */
			,REG_ID	/* 등록자 ID, VARCHAR2(20) */
			,REG_DT	/* 등록 일시, VARCHAR2(14) */
		) VALUES (
			 #{prmCd, jdbcType=VARCHAR}
			,#{channelCd, jdbcType=VARCHAR}
			,#{regId, jdbcType=VARCHAR}
			,#{regDt, jdbcType=VARCHAR}
		)
</update>

<delete id="deleteEzcPrmChannel" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmChannel">
DELETE FROM EZC_PRM_CHANNEL /* 프로모션 채널 */
WHERE 1=1
	AND PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
	AND CHANNEL_CD = #{channelCd, jdbcType=VARCHAR}	/* 채널 코드, VARCHAR2(10) */
</delete>


</mapper>
