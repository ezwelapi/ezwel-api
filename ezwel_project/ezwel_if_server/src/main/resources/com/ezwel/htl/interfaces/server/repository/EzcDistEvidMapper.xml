<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezwel.htl.interfaces.server.repository.EzcDistEvidMapper">
<!--
 * History : SQL Mapper Generated By iCodeManager Made by API Team KSW
 *
 * 버전          성명                   일자              		변경내용
 * =======       ================       ===================		=================
 * 0.0.1         iCodeManager     2018-12-03 15:11:03 	신규작성 
 * 
 * @since 2018-12-03 15:11:03
 * @version 0.0.1
 * @author ksw
 * @see "DBIO Mapper"
 * -->
<insert id="insertEzcDistEvid" parameterType="ezcDistEvid">
INSERT INTO EZC_DIST_EVID(	/* 직영/숙박 증빙 파일 */
	 FACL_EVID_SEQ	/* 시설 증빙 일련번호, NUMBER(0,22) */
	,FACL_CD	/* 시설 코드, NUMBER(0,22) */
	,FACL_EVID_TYPE	/* 시설 증빙 유형, VARCHAR2(8) */
	,FILE_PATH	/* 파일 경로, VARCHAR2(100) */
	,SYS_FILENM	/* 시스템 파일명, VARCHAR2(100) */
	,FILENM	/* 파일명, VARCHAR2(100) */
	,REG_ID	/* 등록자 ID, VARCHAR2(20) */
	,REG_DT	/* 등록 일시, VARCHAR2(14) */
) VALUES (
	 #{faclEvidSeq, jdbcType=DECIMAL}
	,#{faclCd, jdbcType=DECIMAL}
	,#{faclEvidType, jdbcType=VARCHAR}
	,#{filePath, jdbcType=VARCHAR}
	,#{sysFilenm, jdbcType=VARCHAR}
	,#{filenm, jdbcType=VARCHAR}
	,#{regId, jdbcType=VARCHAR}
	,#{regDt, jdbcType=VARCHAR}
)
</insert>

<select id="selectEzcDistEvid" parameterType="ezcDistEvid" resultType="ezcDistEvid">
SELECT
	 EZC_DIST_EVID.FACL_EVID_SEQ AS faclEvidSeq	/* 시설 증빙 일련번호, NUMBER(0,22) */
	,EZC_DIST_EVID.FACL_CD AS faclCd	/* 시설 코드, NUMBER(0,22) */
	,EZC_DIST_EVID.FACL_EVID_TYPE AS faclEvidType	/* 시설 증빙 유형, VARCHAR2(8) */
	,EZC_DIST_EVID.FILE_PATH AS filePath	/* 파일 경로, VARCHAR2(100) */
	,EZC_DIST_EVID.SYS_FILENM AS sysFilenm	/* 시스템 파일명, VARCHAR2(100) */
	,EZC_DIST_EVID.FILENM AS filenm	/* 파일명, VARCHAR2(100) */
	,EZC_DIST_EVID.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
	,EZC_DIST_EVID.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
	,EZC_DIST_EVID.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
	,EZC_DIST_EVID.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
FROM
	 EZC_DIST_EVID	/* 직영/숙박 증빙 파일 */
	,EZC_FACL	/* 시설 정보 */
	,EZC_AREA_CD	/* 지역 코드 */
	,EZC_CITY_CD	/* 도시 코드 */
	,EZC_PARTNER	/* 제휴사 정보 */
WHERE 1=1
	AND EZC_DIST_EVID.FACL_CD = EZC_FACL.FACL_CD
	AND EZC_FACL.AREA_CD = EZC_AREA_CD.AREA_CD
	AND EZC_AREA_CD.CITY_CD = EZC_CITY_CD.CITY_CD
	AND EZC_FACL.CITY_CD = EZC_CITY_CD.CITY_CD
	AND EZC_FACL.PARTNER_CD = EZC_PARTNER.PARTNER_CD
	AND EZC_DIST_EVID.FACL_EVID_SEQ = #{faclEvidSeq, jdbcType=DECIMAL}	/* 시설 증빙 일련번호, NUMBER(0,22), EZC_DIST_EVID_PK(P),SYS_C0011398(C) */
	AND EZC_DIST_EVID.FACL_CD = #{faclCd, jdbcType=DECIMAL}	/* 시설 코드, NUMBER(0,22), FK_EZC_FACL_EZC_DIST_EVID(R),SYS_C0011399(C) */
</select>

<select id="selectCountEzcDistEvid" parameterType="ezcDistEvid" resultType="java.lang.Integer">
SELECT 
	COUNT(1)
FROM
	 EZC_DIST_EVID	/* 직영/숙박 증빙 파일 */
	,EZC_FACL	/* 시설 정보 */
	,EZC_AREA_CD	/* 지역 코드 */
	,EZC_CITY_CD	/* 도시 코드 */
	,EZC_PARTNER	/* 제휴사 정보 */
WHERE 1=1
	AND EZC_DIST_EVID.FACL_CD = EZC_FACL.FACL_CD
	AND EZC_FACL.AREA_CD = EZC_AREA_CD.AREA_CD
	AND EZC_AREA_CD.CITY_CD = EZC_CITY_CD.CITY_CD
	AND EZC_FACL.CITY_CD = EZC_CITY_CD.CITY_CD
	AND EZC_FACL.PARTNER_CD = EZC_PARTNER.PARTNER_CD
	<if test="faclEvidSeq != null and faclEvidSeq gt 0">
	AND EZC_DIST_EVID.FACL_EVID_SEQ = #{faclEvidSeq, jdbcType=DECIMAL}	/* 시설 증빙 일련번호, NUMBER(0,22) */
	</if>
	<if test="faclCd != null and faclCd gt 0">
	AND EZC_DIST_EVID.FACL_CD = #{faclCd, jdbcType=DECIMAL}	/* 시설 코드, NUMBER(0,22) */
	</if>
	<if test="faclEvidType != null and faclEvidType != ''">
	AND EZC_DIST_EVID.FACL_EVID_TYPE = #{faclEvidType, jdbcType=VARCHAR}	/* 시설 증빙 유형, VARCHAR2(8) */
	</if>
	<if test="filePath != null and filePath != ''">
	AND EZC_DIST_EVID.FILE_PATH = #{filePath, jdbcType=VARCHAR}	/* 파일 경로, VARCHAR2(100) */
	</if>
	<if test="sysFilenm != null and sysFilenm != ''">
	AND EZC_DIST_EVID.SYS_FILENM = #{sysFilenm, jdbcType=VARCHAR}	/* 시스템 파일명, VARCHAR2(100) */
	</if>
	<if test="filenm != null and filenm != ''">
	AND EZC_DIST_EVID.FILENM = #{filenm, jdbcType=VARCHAR}	/* 파일명, VARCHAR2(100) */
	</if>
	<if test="regId != null and regId != ''">
	AND EZC_DIST_EVID.REG_ID = #{regId, jdbcType=VARCHAR}	/* 등록자 ID, VARCHAR2(20) */
	</if>
	<if test="regDt != null and regDt != ''">
	AND EZC_DIST_EVID.REG_DT = #{regDt, jdbcType=VARCHAR}	/* 등록 일시, VARCHAR2(14) */
	</if>
	<if test="modiId != null and modiId != ''">
	AND EZC_DIST_EVID.MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
	</if>
	<if test="modiDt != null and modiDt != ''">
	AND EZC_DIST_EVID.MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
	</if>
</select>

<select id="selectListEzcDistEvid" resultType="ezcDistEvid">
SELECT * FROM (
	SELECT ROWNUM AS ROW__NUM, RECORDS.* FROM (
		/* #### SQL Body [[ ################# */
		SELECT
			 EZC_DIST_EVID.FACL_EVID_SEQ AS faclEvidSeq	/* 시설 증빙 일련번호, NUMBER(0,22) */
			,EZC_DIST_EVID.FACL_CD AS faclCd	/* 시설 코드, NUMBER(0,22) */
			,EZC_DIST_EVID.FACL_EVID_TYPE AS faclEvidType	/* 시설 증빙 유형, VARCHAR2(8) */
			,EZC_DIST_EVID.FILE_PATH AS filePath	/* 파일 경로, VARCHAR2(100) */
			,EZC_DIST_EVID.SYS_FILENM AS sysFilenm	/* 시스템 파일명, VARCHAR2(100) */
			,EZC_DIST_EVID.FILENM AS filenm	/* 파일명, VARCHAR2(100) */
			,EZC_DIST_EVID.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
			,EZC_DIST_EVID.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
			,EZC_DIST_EVID.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
			,EZC_DIST_EVID.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
		FROM
			 EZC_DIST_EVID	/* 직영/숙박 증빙 파일 */
			,EZC_FACL	/* 시설 정보 */
			,EZC_AREA_CD	/* 지역 코드 */
			,EZC_CITY_CD	/* 도시 코드 */
			,EZC_PARTNER	/* 제휴사 정보 */
		WHERE 1=1
			AND EZC_DIST_EVID.FACL_CD = EZC_FACL.FACL_CD
			AND EZC_FACL.AREA_CD = EZC_AREA_CD.AREA_CD
			AND EZC_AREA_CD.CITY_CD = EZC_CITY_CD.CITY_CD
			AND EZC_FACL.CITY_CD = EZC_CITY_CD.CITY_CD
			AND EZC_FACL.PARTNER_CD = EZC_PARTNER.PARTNER_CD
			<if test="faclEvidSeq != null and faclEvidSeq gt 0">
			AND EZC_DIST_EVID.FACL_EVID_SEQ = #{faclEvidSeq, jdbcType=DECIMAL}	/* 시설 증빙 일련번호, NUMBER(0,22) */
			</if>
			<if test="faclCd != null and faclCd gt 0">
			AND EZC_DIST_EVID.FACL_CD = #{faclCd, jdbcType=DECIMAL}	/* 시설 코드, NUMBER(0,22) */
			</if>
			<if test="faclEvidType != null and faclEvidType != ''">
			AND EZC_DIST_EVID.FACL_EVID_TYPE = #{faclEvidType, jdbcType=VARCHAR}	/* 시설 증빙 유형, VARCHAR2(8) */
			</if>
			<if test="filePath != null and filePath != ''">
			AND EZC_DIST_EVID.FILE_PATH = #{filePath, jdbcType=VARCHAR}	/* 파일 경로, VARCHAR2(100) */
			</if>
			<if test="sysFilenm != null and sysFilenm != ''">
			AND EZC_DIST_EVID.SYS_FILENM = #{sysFilenm, jdbcType=VARCHAR}	/* 시스템 파일명, VARCHAR2(100) */
			</if>
			<if test="filenm != null and filenm != ''">
			AND EZC_DIST_EVID.FILENM = #{filenm, jdbcType=VARCHAR}	/* 파일명, VARCHAR2(100) */
			</if>
			<if test="regId != null and regId != ''">
			AND EZC_DIST_EVID.REG_ID = #{regId, jdbcType=VARCHAR}	/* 등록자 ID, VARCHAR2(20) */
			</if>
			<if test="regDt != null and regDt != ''">
			AND EZC_DIST_EVID.REG_DT = #{regDt, jdbcType=VARCHAR}	/* 등록 일시, VARCHAR2(14) */
			</if>
			<if test="modiId != null and modiId != ''">
			AND EZC_DIST_EVID.MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
			</if>
			<if test="modiDt != null and modiDt != ''">
			AND EZC_DIST_EVID.MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
			</if>
		ORDER BY
			EZC_DIST_EVID.FACL_EVID_SEQ DESC
		/* #### SQL Body ]] ################# */
	) RECORDS
	WHERE ROWNUM &lt;= ((#{pageNum, jdbcType=NUMERIC}*#{pageCount, jdbcType=NUMERIC})+1) 
)
WHERE ROW__NUM &gt; (#{pageNum, jdbcType=NUMERIC}-1)*#{pageCount, jdbcType=NUMERIC}
</select>

<update id="updateEzcDistEvid" parameterType="ezcDistEvid">
UPDATE EZC_DIST_EVID SET	/* 직영/숙박 증빙 파일 */
	 FACL_EVID_TYPE = #{faclEvidType, jdbcType=VARCHAR}	/* 시설 증빙 유형, VARCHAR2(8) */
	,FILE_PATH = #{filePath, jdbcType=VARCHAR}	/* 파일 경로, VARCHAR2(100) */
	,SYS_FILENM = #{sysFilenm, jdbcType=VARCHAR}	/* 시스템 파일명, VARCHAR2(100) */
	,FILENM = #{filenm, jdbcType=VARCHAR}	/* 파일명, VARCHAR2(100) */
	,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
	,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
WHERE 1=1
	AND FACL_EVID_SEQ = #{faclEvidSeq, jdbcType=DECIMAL}	/* 시설 증빙 일련번호, NUMBER(0,22) */
	AND FACL_CD = #{faclCd, jdbcType=DECIMAL}	/* 시설 코드, NUMBER(0,22) */
</update>

<update id="mergeEzcDistEvid" parameterType="ezcDistEvid">
MERGE INTO EZC_DIST_EVID /* 직영/숙박 증빙 파일 */
	USING DUAL ON (
		FACL_EVID_SEQ = #{faclEvidSeq, jdbcType=DECIMAL}	/* 시설 증빙 일련번호, NUMBER(0,22) */
		AND FACL_CD = #{faclCd, jdbcType=DECIMAL}	/* 시설 코드, NUMBER(0,22) */
	)
	WHEN MATCHED THEN
		UPDATE SET
			 FACL_EVID_TYPE = #{faclEvidType, jdbcType=VARCHAR}	/* 시설 증빙 유형, VARCHAR2(8) */
			,FILE_PATH = #{filePath, jdbcType=VARCHAR}	/* 파일 경로, VARCHAR2(100) */
			,SYS_FILENM = #{sysFilenm, jdbcType=VARCHAR}	/* 시스템 파일명, VARCHAR2(100) */
			,FILENM = #{filenm, jdbcType=VARCHAR}	/* 파일명, VARCHAR2(100) */
			,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
			,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
	WHEN NOT MATCHED THEN
		INSERT (
			 FACL_EVID_SEQ	/* 시설 증빙 일련번호, NUMBER(0,22) */
			,FACL_CD	/* 시설 코드, NUMBER(0,22) */
			,FACL_EVID_TYPE	/* 시설 증빙 유형, VARCHAR2(8) */
			,FILE_PATH	/* 파일 경로, VARCHAR2(100) */
			,SYS_FILENM	/* 시스템 파일명, VARCHAR2(100) */
			,FILENM	/* 파일명, VARCHAR2(100) */
			,REG_ID	/* 등록자 ID, VARCHAR2(20) */
			,REG_DT	/* 등록 일시, VARCHAR2(14) */
		) VALUES (
			 #{faclEvidSeq, jdbcType=DECIMAL}
			,#{faclCd, jdbcType=DECIMAL}
			,#{faclEvidType, jdbcType=VARCHAR}
			,#{filePath, jdbcType=VARCHAR}
			,#{sysFilenm, jdbcType=VARCHAR}
			,#{filenm, jdbcType=VARCHAR}
			,#{regId, jdbcType=VARCHAR}
			,#{regDt, jdbcType=VARCHAR}
		)
</update>

<delete id="deleteEzcDistEvid" parameterType="ezcDistEvid">
DELETE FROM EZC_DIST_EVID /* 직영/숙박 증빙 파일 */
WHERE 1=1
	AND FACL_EVID_SEQ = #{faclEvidSeq, jdbcType=DECIMAL}	/* 시설 증빙 일련번호, NUMBER(0,22) */
	AND FACL_CD = #{faclCd, jdbcType=DECIMAL}	/* 시설 코드, NUMBER(0,22) */
</delete>


</mapper>
