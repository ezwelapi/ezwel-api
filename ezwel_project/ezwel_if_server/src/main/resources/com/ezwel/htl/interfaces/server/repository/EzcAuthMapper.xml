<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezwel.htl.interfaces.server.repository.EzcAuthMapper">
<!--
 * History : SQL Mapper Generated By iCodeManager Made by API Team KSW
 *
 * 버전          성명                   일자              		변경내용
 * =======       ================       ===================		=================
 * 0.0.1         CodeSkeleton (ksw)     2018-12-03 11:05:01 	신규작성 
 * 
 * @since 2018-12-03 11:05:01
 * @version 0.0.1
 * @author ksw
 * @see "DBIO Mapper"
 * -->
<insert id="insertEzcAuth" parameterType="com.ezwel.htl.interfaces.server.entities.EzcAuth">
INSERT INTO EZC_AUTH(	/* 권한 정보 */
	 MENU_ID	/* 메뉴 ID, VARCHAR2(6) */
	,AUTH_GRP_CD	/* 권한 그룹 코드, VARCHAR2(10) */
	,AUTH_C	/* 권한 C, CHAR(1) */
	,AUTH_R	/* 권한 R, CHAR(1) */
	,AUTH_U	/* 권한 U, CHAR(1) */
	,AUTH_D	/* 권한 D, CHAR(1) */
	,AUTH_X	/* 권한 X, CHAR(1) */
	,AUTH_P	/* 권한 P, CHAR(1) */
	,REG_ID	/* 등록자 ID, VARCHAR2(20) */
	,REG_DT	/* 등록 일시, VARCHAR2(14) */
) VALUES (
	 #{menuId, jdbcType=VARCHAR}
	,#{authGrpCd, jdbcType=VARCHAR}
	,#{authC, jdbcType=CHAR}
	,#{authR, jdbcType=CHAR}
	,#{authU, jdbcType=CHAR}
	,#{authD, jdbcType=CHAR}
	,#{authX, jdbcType=CHAR}
	,#{authP, jdbcType=CHAR}
	,#{regId, jdbcType=VARCHAR}
	,#{regDt, jdbcType=VARCHAR}
)
</insert>

<select id="selectEzcAuth" parameterType="com.ezwel.htl.interfaces.server.entities.EzcAuth" resultType="com.ezwel.htl.interfaces.server.entities.EzcAuth">
SELECT
	 EZC_AUTH.MENU_ID AS menuId	/* 메뉴 ID, VARCHAR2(6) */
	,EZC_AUTH.AUTH_GRP_CD AS authGrpCd	/* 권한 그룹 코드, VARCHAR2(10) */
	,EZC_AUTH.AUTH_C AS authC	/* 권한 C, CHAR(1) */
	,EZC_AUTH.AUTH_R AS authR	/* 권한 R, CHAR(1) */
	,EZC_AUTH.AUTH_U AS authU	/* 권한 U, CHAR(1) */
	,EZC_AUTH.AUTH_D AS authD	/* 권한 D, CHAR(1) */
	,EZC_AUTH.AUTH_X AS authX	/* 권한 X, CHAR(1) */
	,EZC_AUTH.AUTH_P AS authP	/* 권한 P, CHAR(1) */
	,EZC_AUTH.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
	,EZC_AUTH.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
	,EZC_AUTH.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
	,EZC_AUTH.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
FROM
	 EZC_AUTH	/* 권한 정보 */
	,EZC_AUTH_GRP	/* 권한 그룹 */
	,EZC_MENU	/* 메뉴 정보 */
WHERE 1=1
	AND EZC_AUTH.AUTH_GRP_CD = EZC_AUTH_GRP.AUTH_GRP_CD
	AND EZC_AUTH.MENU_ID = EZC_MENU.MENU_ID
	AND EZC_AUTH.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_AUTH_GRP.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_MENU.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_AUTH.MENU_ID = #{menuId, jdbcType=VARCHAR}	/* 메뉴 ID, VARCHAR2(6) */
	AND EZC_AUTH.AUTH_GRP_CD = #{authGrpCd, jdbcType=VARCHAR}	/* 권한 그룹 코드, VARCHAR2(10) */
</select>

<select id="selectCountEzcAuth" parameterType="com.ezwel.htl.interfaces.server.entities.EzcAuth" resultType="java.lang.Integer">
SELECT 
	COUNT(1)
FROM
	 EZC_AUTH	/* 권한 정보 */
	,EZC_AUTH_GRP	/* 권한 그룹 */
	,EZC_MENU	/* 메뉴 정보 */
WHERE 1=1
	AND EZC_AUTH.AUTH_GRP_CD = EZC_AUTH_GRP.AUTH_GRP_CD
	AND EZC_AUTH.MENU_ID = EZC_MENU.MENU_ID
	AND EZC_AUTH.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_AUTH_GRP.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_MENU.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	<if test="menuId != null and menuId != ''">
	AND EZC_AUTH.MENU_ID = #{menuId, jdbcType=VARCHAR}	/* 메뉴 ID, VARCHAR2(6) */
	</if>
	<if test="authGrpCd != null and authGrpCd != ''">
	AND EZC_AUTH.AUTH_GRP_CD = #{authGrpCd, jdbcType=VARCHAR}	/* 권한 그룹 코드, VARCHAR2(10) */
	</if>
</select>

<select id="selectListEzcAuth" resultType="com.ezwel.htl.interfaces.server.entities.EzcAuth">
SELECT * FROM (
	SELECT ROWNUM AS ROW__NUM, RECORDS.* FROM (
		/* #### SQL Body [[ ################# */
		SELECT
			 EZC_AUTH.MENU_ID AS menuId	/* 메뉴 ID, VARCHAR2(6) */
			,EZC_AUTH.AUTH_GRP_CD AS authGrpCd	/* 권한 그룹 코드, VARCHAR2(10) */
			,EZC_AUTH.AUTH_C AS authC	/* 권한 C, CHAR(1) */
			,EZC_AUTH.AUTH_R AS authR	/* 권한 R, CHAR(1) */
			,EZC_AUTH.AUTH_U AS authU	/* 권한 U, CHAR(1) */
			,EZC_AUTH.AUTH_D AS authD	/* 권한 D, CHAR(1) */
			,EZC_AUTH.AUTH_X AS authX	/* 권한 X, CHAR(1) */
			,EZC_AUTH.AUTH_P AS authP	/* 권한 P, CHAR(1) */
			,EZC_AUTH.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
			,EZC_AUTH.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
			,EZC_AUTH.MODI_ID AS modiId	/* 수정자 ID, VARCHAR2(20) */
			,EZC_AUTH.MODI_DT AS modiDt	/* 수정 일시, VARCHAR2(14) */
		FROM
			 EZC_AUTH	/* 권한 정보 */
			,EZC_AUTH_GRP	/* 권한 그룹 */
			,EZC_MENU	/* 메뉴 정보 */
		WHERE 1=1
			AND EZC_AUTH.AUTH_GRP_CD = EZC_AUTH_GRP.AUTH_GRP_CD
			AND EZC_AUTH.MENU_ID = EZC_MENU.MENU_ID
			AND EZC_AUTH.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			AND EZC_AUTH_GRP.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			AND EZC_MENU.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			<if test="param.menuId != null and param.menuId != ''">
			AND EZC_AUTH.MENU_ID = #{param.menuId, jdbcType=VARCHAR}	/* 메뉴 ID, VARCHAR2(6) */
			</if>
			<if test="param.authGrpCd != null and param.authGrpCd != ''">
			AND EZC_AUTH.AUTH_GRP_CD = #{param.authGrpCd, jdbcType=VARCHAR}	/* 권한 그룹 코드, VARCHAR2(10) */
			</if>
		ORDER BY
			EZC_AUTH.MENU_ID DESC
		/* #### SQL Body ]] ################# */
	) RECORDS
	WHERE ROWNUM &lt;= ((#{param.pageNum, jdbcType=NUMERIC}*#{param.pageCount, jdbcType=NUMERIC})+1) 
)
WHERE ROW__NUM &gt; (#{param.pageNum, jdbcType=NUMERIC}-1)*#{param.pageCount, jdbcType=NUMERIC}
</select>

<update id="updateEzcAuth" parameterType="com.ezwel.htl.interfaces.server.entities.EzcAuth">
UPDATE EZC_AUTH SET	/* 권한 정보 */
	 AUTH_C = #{authC, jdbcType=CHAR}	/* 권한 C, CHAR(1) */
	,AUTH_R = #{authR, jdbcType=CHAR}	/* 권한 R, CHAR(1) */
	,AUTH_U = #{authU, jdbcType=CHAR}	/* 권한 U, CHAR(1) */
	,AUTH_D = #{authD, jdbcType=CHAR}	/* 권한 D, CHAR(1) */
	,AUTH_X = #{authX, jdbcType=CHAR}	/* 권한 X, CHAR(1) */
	,AUTH_P = #{authP, jdbcType=CHAR}	/* 권한 P, CHAR(1) */
	,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
	,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
WHERE 1=1
	AND MENU_ID = #{menuId, jdbcType=VARCHAR}	/* 메뉴 ID, VARCHAR2(6) */
	AND AUTH_GRP_CD = #{authGrpCd, jdbcType=VARCHAR}	/* 권한 그룹 코드, VARCHAR2(10) */
</update>

<update id="mergeEzcAuth" parameterType="com.ezwel.htl.interfaces.server.entities.EzcAuth">
MERGE INTO EZC_AUTH /* 권한 정보 */
	USING DUAL ON (
		MENU_ID = #{menuId, jdbcType=VARCHAR}	/* 메뉴 ID, VARCHAR2(6) */
		AND AUTH_GRP_CD = #{authGrpCd, jdbcType=VARCHAR}	/* 권한 그룹 코드, VARCHAR2(10) */
	)
	WHEN MATCHED THEN
		UPDATE SET
			 AUTH_C = #{authC, jdbcType=CHAR}	/* 권한 C, CHAR(1) */
			,AUTH_R = #{authR, jdbcType=CHAR}	/* 권한 R, CHAR(1) */
			,AUTH_U = #{authU, jdbcType=CHAR}	/* 권한 U, CHAR(1) */
			,AUTH_D = #{authD, jdbcType=CHAR}	/* 권한 D, CHAR(1) */
			,AUTH_X = #{authX, jdbcType=CHAR}	/* 권한 X, CHAR(1) */
			,AUTH_P = #{authP, jdbcType=CHAR}	/* 권한 P, CHAR(1) */
			,MODI_ID = #{modiId, jdbcType=VARCHAR}	/* 수정자 ID, VARCHAR2(20) */
			,MODI_DT = #{modiDt, jdbcType=VARCHAR}	/* 수정 일시, VARCHAR2(14) */
	WHEN NOT MATCHED THEN
		INSERT (
			 MENU_ID	/* 메뉴 ID, VARCHAR2(6) */
			,AUTH_GRP_CD	/* 권한 그룹 코드, VARCHAR2(10) */
			,AUTH_C	/* 권한 C, CHAR(1) */
			,AUTH_R	/* 권한 R, CHAR(1) */
			,AUTH_U	/* 권한 U, CHAR(1) */
			,AUTH_D	/* 권한 D, CHAR(1) */
			,AUTH_X	/* 권한 X, CHAR(1) */
			,AUTH_P	/* 권한 P, CHAR(1) */
			,REG_ID	/* 등록자 ID, VARCHAR2(20) */
			,REG_DT	/* 등록 일시, VARCHAR2(14) */
		) VALUES (
			 #{menuId, jdbcType=VARCHAR}
			,#{authGrpCd, jdbcType=VARCHAR}
			,#{authC, jdbcType=CHAR}
			,#{authR, jdbcType=CHAR}
			,#{authU, jdbcType=CHAR}
			,#{authD, jdbcType=CHAR}
			,#{authX, jdbcType=CHAR}
			,#{authP, jdbcType=CHAR}
			,#{regId, jdbcType=VARCHAR}
			,#{regDt, jdbcType=VARCHAR}
		)
</update>

<delete id="deleteEzcAuth" parameterType="com.ezwel.htl.interfaces.server.entities.EzcAuth">
DELETE FROM EZC_AUTH /* 권한 정보 */
WHERE 1=1
	AND MENU_ID = #{menuId, jdbcType=VARCHAR}	/* 메뉴 ID, VARCHAR2(6) */
	AND AUTH_GRP_CD = #{authGrpCd, jdbcType=VARCHAR}	/* 권한 그룹 코드, VARCHAR2(10) */
</delete>


</mapper>
