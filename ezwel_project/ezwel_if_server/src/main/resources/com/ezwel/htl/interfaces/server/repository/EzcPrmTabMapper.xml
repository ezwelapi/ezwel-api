<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezwel.htl.interfaces.server.repository.EzcPrmTabMapper">
<!--
 * History : SQL Mapper Generated By iCodeManager Made by API Team KSW
 *
 * 버전          성명                   일자              		변경내용
 * =======       ================       ===================		=================
 * 0.0.1         CodeSkeleton (ksw)     2018-12-03 11:05:04 	신규작성 
 * 
 * @since 2018-12-03 11:05:04
 * @version 0.0.1
 * @author ksw
 * @see "DBIO Mapper"
 * -->
<insert id="insertEzcPrmTab" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmTab">
INSERT INTO EZC_PRM_TAB(	/* 프로모션 TAB */
	 TAB_SEQ	/* TAB 일련번호, NUMBER(0,22) */
	,PRM_CD	/* 프로모션 코드, VARCHAR2(10) */
	,TAB_NM	/* TAB 명, VARCHAR2(100) */
	,GOODS_DISP_ARR	/* 상품 노출 배열, NUMBER(0,22) */
	,REG_ID	/* 등록자 ID, VARCHAR2(20) */
	,REG_DT	/* 등록 일시, VARCHAR2(14) */
) VALUES (
	 #{tabSeq, jdbcType=DECIMAL}
	,#{prmCd, jdbcType=VARCHAR}
	,#{tabNm, jdbcType=VARCHAR}
	,#{goodsDispArr, jdbcType=NUMERIC}
	,#{regId, jdbcType=VARCHAR}
	,#{regDt, jdbcType=VARCHAR}
)
</insert>

<select id="selectEzcPrmTab" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmTab" resultType="com.ezwel.htl.interfaces.server.entities.EzcPrmTab">
SELECT
	 EZC_PRM_TAB.TAB_SEQ AS tabSeq	/* TAB 일련번호, NUMBER(0,22) */
	,EZC_PRM_TAB.PRM_CD AS prmCd	/* 프로모션 코드, VARCHAR2(10) */
	,EZC_PRM_TAB.TAB_NM AS tabNm	/* TAB 명, VARCHAR2(100) */
	,EZC_PRM_TAB.GOODS_DISP_ARR AS goodsDispArr	/* 상품 노출 배열, NUMBER(0,22) */
	,EZC_PRM_TAB.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
	,EZC_PRM_TAB.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
FROM
	 EZC_PRM_TAB	/* 프로모션 TAB */
	,EZC_PRM_MNG	/* 프로모션 관리 */
WHERE 1=1
	AND EZC_PRM_TAB.PRM_CD = EZC_PRM_MNG.PRM_CD
	AND EZC_PRM_TAB.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_PRM_MNG.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_PRM_TAB.TAB_SEQ = #{tabSeq, jdbcType=DECIMAL}	/* TAB 일련번호, NUMBER(0,22) */
	AND EZC_PRM_TAB.PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
</select>

<select id="selectCountEzcPrmTab" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmTab" resultType="java.lang.Integer">
SELECT 
	COUNT(1)
FROM
	 EZC_PRM_TAB	/* 프로모션 TAB */
	,EZC_PRM_MNG	/* 프로모션 관리 */
WHERE 1=1
	AND EZC_PRM_TAB.PRM_CD = EZC_PRM_MNG.PRM_CD
	AND EZC_PRM_TAB.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	AND EZC_PRM_MNG.REC_STAT = #{recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
	<if test="tabSeq != null and tabSeq gt 0">
	AND EZC_PRM_TAB.TAB_SEQ = #{tabSeq, jdbcType=DECIMAL}	/* TAB 일련번호, NUMBER(0,22) */
	</if>
	<if test="prmCd != null and prmCd != ''">
	AND EZC_PRM_TAB.PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
	</if>
</select>

<select id="selectListEzcPrmTab" resultType="com.ezwel.htl.interfaces.server.entities.EzcPrmTab">
SELECT * FROM (
	SELECT ROWNUM AS ROW__NUM, RECORDS.* FROM (
		/* #### SQL Body [[ ################# */
		SELECT
			 EZC_PRM_TAB.TAB_SEQ AS tabSeq	/* TAB 일련번호, NUMBER(0,22) */
			,EZC_PRM_TAB.PRM_CD AS prmCd	/* 프로모션 코드, VARCHAR2(10) */
			,EZC_PRM_TAB.TAB_NM AS tabNm	/* TAB 명, VARCHAR2(100) */
			,EZC_PRM_TAB.GOODS_DISP_ARR AS goodsDispArr	/* 상품 노출 배열, NUMBER(0,22) */
			,EZC_PRM_TAB.REG_ID AS regId	/* 등록자 ID, VARCHAR2(20) */
			,EZC_PRM_TAB.REG_DT AS regDt	/* 등록 일시, VARCHAR2(14) */
		FROM
			 EZC_PRM_TAB	/* 프로모션 TAB */
			,EZC_PRM_MNG	/* 프로모션 관리 */
		WHERE 1=1
			AND EZC_PRM_TAB.PRM_CD = EZC_PRM_MNG.PRM_CD
			AND EZC_PRM_TAB.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			AND EZC_PRM_MNG.REC_STAT = #{param.recStat, jdbcType=CHAR}	/* 레코드 상태, CHAR(1) */
			<if test="param.tabSeq != null and param.tabSeq gt 0">
			AND EZC_PRM_TAB.TAB_SEQ = #{param.tabSeq, jdbcType=DECIMAL}	/* TAB 일련번호, NUMBER(0,22) */
			</if>
			<if test="param.prmCd != null and param.prmCd != ''">
			AND EZC_PRM_TAB.PRM_CD = #{param.prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
			</if>
		ORDER BY
			EZC_PRM_TAB.TAB_SEQ DESC
		/* #### SQL Body ]] ################# */
	) RECORDS
	WHERE ROWNUM &lt;= ((#{param.pageNum, jdbcType=NUMERIC}*#{param.pageCount, jdbcType=NUMERIC})+1) 
)
WHERE ROW__NUM &gt; (#{param.pageNum, jdbcType=NUMERIC}-1)*#{param.pageCount, jdbcType=NUMERIC}
</select>

<update id="updateEzcPrmTab" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmTab">
UPDATE EZC_PRM_TAB SET	/* 프로모션 TAB */
	 TAB_NM = #{tabNm, jdbcType=VARCHAR}	/* TAB 명, VARCHAR2(100) */
	,GOODS_DISP_ARR = #{goodsDispArr, jdbcType=NUMERIC}	/* 상품 노출 배열, NUMBER(0,22) */
WHERE 1=1
	AND TAB_SEQ = #{tabSeq, jdbcType=DECIMAL}	/* TAB 일련번호, NUMBER(0,22) */
	AND PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
</update>

<update id="mergeEzcPrmTab" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmTab">
MERGE INTO EZC_PRM_TAB /* 프로모션 TAB */
	USING DUAL ON (
		TAB_SEQ = #{tabSeq, jdbcType=DECIMAL}	/* TAB 일련번호, NUMBER(0,22) */
		AND PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
	)
	WHEN MATCHED THEN
		UPDATE SET
			 TAB_NM = #{tabNm, jdbcType=VARCHAR}	/* TAB 명, VARCHAR2(100) */
			,GOODS_DISP_ARR = #{goodsDispArr, jdbcType=NUMERIC}	/* 상품 노출 배열, NUMBER(0,22) */
	WHEN NOT MATCHED THEN
		INSERT (
			 TAB_SEQ	/* TAB 일련번호, NUMBER(0,22) */
			,PRM_CD	/* 프로모션 코드, VARCHAR2(10) */
			,TAB_NM	/* TAB 명, VARCHAR2(100) */
			,GOODS_DISP_ARR	/* 상품 노출 배열, NUMBER(0,22) */
			,REG_ID	/* 등록자 ID, VARCHAR2(20) */
			,REG_DT	/* 등록 일시, VARCHAR2(14) */
		) VALUES (
			 #{tabSeq, jdbcType=DECIMAL}
			,#{prmCd, jdbcType=VARCHAR}
			,#{tabNm, jdbcType=VARCHAR}
			,#{goodsDispArr, jdbcType=NUMERIC}
			,#{regId, jdbcType=VARCHAR}
			,#{regDt, jdbcType=VARCHAR}
		)
</update>

<delete id="deleteEzcPrmTab" parameterType="com.ezwel.htl.interfaces.server.entities.EzcPrmTab">
DELETE FROM EZC_PRM_TAB /* 프로모션 TAB */
WHERE 1=1
	AND TAB_SEQ = #{tabSeq, jdbcType=DECIMAL}	/* TAB 일련번호, NUMBER(0,22) */
	AND PRM_CD = #{prmCd, jdbcType=VARCHAR}	/* 프로모션 코드, VARCHAR2(10) */
</delete>


</mapper>
